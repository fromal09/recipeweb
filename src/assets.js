/**
 * assets.js
 *
 * Replace the values below with your hosted image URLs,
 * or swap back to base64 strings if you need self-contained builds.
 *
 * Original sizes:
 *   KITCHEN_IMG  ≈ 153 KB  (JPEG — the kitchen background)
 *   NANA_IMG     ≈  97 KB  (PNG  — Nana's portrait)
 *   MAP_IMG      ≈  ?? KB  (JPEG — world map, used in WorldCuisineScreen)
 *
 * Quick options:
 *   1. Put the images in /public/images/ and use "/images/kitchen.jpg" etc.
 *   2. Import them via bundler: import kitchenUrl from "./kitchen.jpg"
 *   3. Re-paste the base64 string here (works but bloats the bundle)
 */

export const KITCHEN_IMG = "/images/kitchen.jpg";   // TODO: host + replace
export const NANA_IMG    = "/images/nana.png";       // TODO: host + replace
export const MAP_IMG     = "/images/world-map.jpg";  // TODO: host + replace
